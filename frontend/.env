# Legacy key (not used by Angular build env exposure)
# BACKEND_URL="http://localhost:3000"

# Angular-exposed env key used in src/environments/environment.ts
NG_APP_BACKEND_URL=https://jspregistration.onrender.com

# Vite-prefixed fallback key (used if NG_APP_* is not exposed)
VITE_BACKEND_URL=https://jspregistration.onrender.com